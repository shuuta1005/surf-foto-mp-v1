import CartTable from "./cart-table";

const CartPage = async () => {
  return (
    //we need to get our cart data
    //const cart = await getCart() something like this and use it in CartTable
    <div>
      <CartTable />
    </div>
  );
};

export default CartPage;
